<?xml version="1.0" encoding="UTF-8"?>
<project name="Hurricane" default="init">

    <taskdef resource="net/sf/antcontrib/antcontrib.properties" />
	<taskdef name="ifcontrib" classname="net.sf.antcontrib.logic.IfTask"/>
	<taskdef name="forcontrib" classname="net.sf.antcontrib.logic.For"/>
	
    <property file="../../build/${build.branch}/commonbuild.properties" />
    <property file="../../build/${build.branch}/version.properties" />
    
    <property name="build.dist" value="../../build/${build.branch}/${product.version}.${build.version}" />
    <property name="proguard.enabled" value="${build.proguard.enabled}" />
	
	<ifcontrib>
        <istrue value="${build.debug}" />
        <then>
            <property file="../../build/${build.branch}/dbg_build.properties" />
        </then>
        <else>
            <property file="../../build/${build.branch}/rel_build.properties" />
        </else>
    </ifcontrib>
    
	<property name="verbose" value="false" />
	
    <!-- The local.properties file is created and updated by the 'android' tool.
         It contains the path to the SDK. It should *NOT* be checked into
         Version Control Systems. -->
    <property file="local.properties" />

    <!-- The ant.properties file can be created by you. It is only edited by the
         'android' tool to add properties to it.
         This is the place to change some Ant specific build properties.
         Here are some properties you may want to change/update:

         source.dir
             The name of the source directory. Default is 'src'.
         out.dir
             The name of the output directory. Default is 'bin'.

         For other overridable properties, look at the beginning of the rules
         files in the SDK, at tools/ant/build.xml

         Properties related to the SDK location or the project target should
         be updated using the 'android' tool with the 'update' action.

         This file is an integral part of the build system for your
         application and should be checked into Version Control Systems.

         -->
    <property file="ant.properties" />
    <property name="sdk.dir" value="${sdk-location}" />

    <!-- if sdk.dir was not set from one of the property file, then
         get it from the ANDROID_HOME env var.
         This must be done before we load project.properties since
         the proguard config can use sdk.dir -->
    <property environment="env" />
    <condition property="sdk.dir" value="${env.ANDROID_HOME}">
        <isset property="env.ANDROID_HOME" />
    </condition>

    <!-- The project.properties file is created and updated by the 'android'
         tool, as well as ADT.

         This contains project specific properties such as project target, and library
         dependencies. Lower level build properties are stored in ant.properties
         (or in .classpath for Eclipse projects).

         This file is an integral part of the build system for your
         application and should be checked into Version Control Systems. -->
    <loadproperties srcFile="project.properties" />
    
    <property name="proguard.config" value="${sdk.dir}/tools/proguard/proguard-android.txt:proguard-project.txt"/>

    <!-- quick check on sdk.dir -->
    <fail
            message="sdk.dir is missing. Make sure to generate local.properties using 'android update project' or to inject it through the ANDROID_HOME environment variable."
            unless="sdk.dir"
    />

    <!--
        Import per project custom build rules if present at the root of the project.
        This is the place to put custom intermediary targets such as:
            -pre-build
            -pre-compile
            -post-compile (This is typically used for code obfuscation.
                           Compiled code location: ${out.classes.absolute.dir}
                           If this is not done in place, override ${out.dex.input.absolute.dir})
            -post-package
            -post-build
            -pre-clean
    -->
    
    <target name="init" depends="-lib-projects-setup, build_app_config_files, clean, post-release" />
    
    <target name="-lib-projects-setup">
        <propertyselector property="hurricane.library.projects" match="android.library.reference\.(.*)" select="\0"/>

        <forcontrib list="${hurricane.library.projects}" param="lib.project.folder">
            <sequential>
                <copy file="../../build/${build.branch}/commonbuild.properties" tofile="${@{lib.project.folder}}/local.properties" overwrite="true" />
                
                <ifcontrib>
                    <available file="${@{lib.project.folder}}/assets" type="dir"/>
                    <then>
                        <copy todir="${asset.dir}" overwrite="true" force="true">
                            <fileset dir="${@{lib.project.folder}}/assets">
                                <include name="**/*"/>
                            </fileset>
                        </copy>
                    </then>
                </ifcontrib>
                
            </sequential>
		</forcontrib>
		
        
    </target>
    
    <target name="post-release" depends="release">
        <ifcontrib>
            <istrue value="${build.debug}" />
            <then>
                <property name="build_type" value="-debug" />
            </then>
            <else>
                <property name="build_type" value="-release" />
            </else>
        </ifcontrib>
        <ifcontrib>
            <istrue value="${proguard.enabled}" />
            <then>
                <property name="obf_flag" value="-obf" />
            </then>
            <else>
                <property name="obf_flag" value="-unobf" />
            </else>
        </ifcontrib>
        <ifcontrib>
            <available file="${out.packaged.file}" type="file"/>
            <then>
                <copy file="${out.packaged.file}" tofile="${build.dist}/unsigned/${ant.project.name}${build_type}${obf_flag}-unsigned.apk" overwrite="true" />
            </then>
        </ifcontrib>
        <ifcontrib>
            <available file="${out.final.file}" type="file"/>
            <then>
                <copy file="${out.final.file}" tofile="${build.dist}/${ant.project.name}${build_type}${obf_flag}-signed.apk"  overwrite="true"/>
            </then>
        </ifcontrib>
<!--        <ifcontrib>
            <available file="${out.unaligned.file}" type="file"/>
            <then>
                <copy file="${out.unaligned.file}" tofile="${build.dist}/${ant.project.name}${build_type}${obf_flag}-unaligned.apk"  overwrite="true"/>
            </then>
        </ifcontrib> -->
    </target>
    

    <!-- Build app config files.
         - AndroidManifest.xml
         - AppBuildConfig.java
    -->
    <import file="build_app_config.xml" />

    <!-- Import the actual build file.

         To customize existing targets, there are two options:
         - Customize only one target:
             - copy/paste the target into this file, *before* the
               <import> task.
             - customize it to your needs.
         - Customize the whole content of build.xml
             - copy/paste the content of the rules files (minus the top node)
               into this file, replacing the <import> task.
             - customize to your needs.

         ***********************
         ****** IMPORTANT ******
         ***********************
         In all cases you must update the value of version-tag below to read 'custom' instead of an integer,
         in order to avoid having your file be overridden by tools such as "android update project"
    -->
    <!-- version-tag: 1 -->
    <import file="${sdk.dir}/tools/ant/build.xml" />

</project>
